import {} from '../type';

const initialState = {

}

export default function restaurantReducer(state = initialState, action) {
    switch(action.type) {
        default:
            return state
    }
}